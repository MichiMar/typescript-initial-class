{
  "compilerOptions": {
    "sourceMap": true
  },
  "files": ["008_type_alias.ts"]
  //  # In the string only the file I want to complid
}

// ts files are going to work only with this or everithing with .ts is going to run
